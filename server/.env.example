# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB (Mandatory)
MONGODB_URI=mongodb://localhost:27017/immortal_zone

# Redis (Mandatory for Caching & Rate Limiting)
REDIS_URL=redis://localhost:6379

# JWT Authentication (Mandatory)
# IMPORTANT: Generate a strong random secret with at least 32 characters
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_token_change_this_immediately

# Client Configuration
# Frontend URL for CORS - REQUIRED in production
CLIENT_URL=http://localhost:5173

# Cloudflare R2 (Mandatory for File Storage)
# Access Key ID & Secret Access Key from Cloudflare R2 dashboard
R2_ACCESS_KEY_ID=your_access_key_id
R2_SECRET_ACCESS_KEY=your_secret_access_key

# R2 Endpoint (found in R2 bucket settings)
R2_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com

# R2 Bucket Name
R2_BUCKET_NAME=your_bucket_name

# R2 Public URL (Custom domain or pub-xxx.r2.dev URL)
R2_PUBLIC_URL=https://your_public_url.com

# SECURITY NOTES:
# 1. NEVER commit .env file to git
# 2. Rotate all credentials if exposed
# 3. Use strong, unique values for JWT_SECRET
# 4. Keep production credentials separate from development
